1. Go to:
	http://www.yesodweb.com/page/quickstart

	1.1 Install the libs at the bottom of the page
	1.2 Install cabal
		sudo apt-get install cabal-install
	1.3 Follow up the steps at the top of the page.

UEBGate Project Setup

1. stack new uebgate yesod-postgres && cd uebgate
